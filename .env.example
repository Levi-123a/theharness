# the-harness 环境变量配置示例
#
# 使用方法：
#   1. 复制本文件为 .env：  cp .env.example .env
#   2. 填入你自己的 API 密钥（不要使用引号）
#   3. .env 已被 .gitignore 排除，不会被提交
#
# 安全说明（务必阅读）：
#   - .env 是明文文件，任何能读取该文件的进程/用户都能看到你的密钥
#   - 进程环境变量对同一进程及其子进程可见
#   - 这是一种开发便利，不是安全存储方案
#   - 生产环境推荐使用 OS 钥匙串（通过 WebUI 设置页或 `the-harness-creds store` 录入）
#
# 查找优先级：OS 钥匙串 (keyring) > 环境变量
# 当钥匙串中已存储某 provider 的凭据时，环境变量将被忽略。

# OpenAI 兼容供应商配置（支持 OpenAI 官方、DeepSeek、Moonshot 等兼容端点）
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# DeepSeek 示例（取消注释并填入你的 DeepSeek key）
# OPENAI_API_KEY=sk-your-deepseek-key-here
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# OPENAI_MODEL=deepseek-chat
